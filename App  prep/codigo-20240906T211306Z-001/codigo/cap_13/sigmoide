import matplotlib.pyplot as plt
import numpy as np

def sigmoide(z):
    resultado = 1.0 / (1.0 + np.exp(-z))
    return resultado

z = np.arange(-10, 10, 0.1)
fi_z = sigmoide(z)
plt.plot(z, fi_z)
plt.axvline(0.0, color='k')
plt.axhspan(ymin, ymax, kwargs)
